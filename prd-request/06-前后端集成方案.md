# 多 Agent 协作科研分析系统 — 前后端集成方案

## 0. 状态跟踪表

> 此表跟踪本 PRD 涉及功能的实现进度，随项目进展持续更新。

| 集成模块 | 前端 | 后端 | 测试 | 用户反馈 | 总体状态 |
|---------|:----:|:----:|:----:|:--------:|:--------:|
| 📦 **LangGraph 依赖安装** | — | ✅ langgraph + langchain | — | — | ✅ **已完成** |
| 🏗️ **agent/ 包结构** | — | ✅ 7个模块 | ✅ 测试通过 | — | ✅ **已完成** |
| 🛠️ **tools/ 包结构** | — | ✅ arxiv_tool | ✅ 测试通过 | — | ✅ **已完成** |
| 🗃️ **memory/ 包结构** | — | ✅ manager.py | ✅ 测试通过 | — | ✅ **已完成** |
| 🌊 **SSE Agent 事件格式** | ✅ | ✅ astream_events 映射 | ✅ 测试通过 | — | ✅ **已完成** |
| 👁️ **AgentTrace 可视化组件** | ✅ | — | ✅ 测试通过 | — | ✅ **已完成** |
| 📋 **验证 Checklist** | — | — | ✅ **8 项测试通过** | — | ✅ **已完成** |

**图例：** ✅ 已完成 &nbsp;🚧 开发中 &nbsp;⏳ 待测试 &nbsp;❌ 未开始 &nbsp;📝 已规划

---

## 1. 集成总览

现有的 FastAPI + React 架构保持不变，核心变化是：

- **后端**：在 `/chat/stream` 端点中嵌入 LangGraph 引擎，替换原来的直接 LLM 调用
- **前端**：增加 Agent 思考过程的可视化展示，保留现有聊天 UI

```
用户 → 前端 React → POST /chat/stream → FastAPI → LangGraph StateGraph
                                                       │
                                          ┌────────────┼────────────┐
                                          ▼            ▼            ▼
                                     Chat Agent  Research Agent  Innovator...
                                          │            │            │
                                          └────────────┼────────────┘
                                                       ▼
                                               LLM API (DeepSeek)
```

## 2. 后端改造

### 2.1 新增依赖（requirements.txt）

```
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
python-docx>=1.1.0
chromadb>=0.5.0          # 可选，长期记忆
```

### 2.2 代码结构

```
backend/
├── main.py                  # FastAPI 入口（基本不变）
├── requirements.txt
├── Dockerfile.backend
├── agent/
│   ├── __init__.py
│   ├── graph.py             # LangGraph 图定义与编译
│   ├── state.py             # AgentState 类型定义
│   ├── supervisor.py         # Supervisor 节点 & 路由
│   ├── chat_agent.py         # 对话 Agent 节点
│   ├── research_agent.py     # 调研 Agent 节点
│   ├── innovator_agent.py    # 创新 Agent 节点
│   ├── experiment_agent.py   # 实验 Agent 节点
│   └── reporter.py           # 报告生成节点
├── tools/
│   ├── __init__.py
│   ├── arxiv_tool.py         # arXiv 搜索工具
│   ├── docx_tool.py          # Word 文档工具
│   └── drawio_tool.py        # Draw.io 绘图工具
└── memory/
    ├── __init__.py
    └── manager.py            # 三层记忆管理器
```

### 2.3 /chat/stream 端点改造

```python
# main.py（修改后）
from agent.graph import AgentGraph

agent_graph = AgentGraph()

@app.post("/chat/stream")
async def chat_stream(request: ChatRequest):
    session_id = request.session_id
    user_message = request.message
    model_name = MODEL_MAP.get(request.model, "deepseek-chat")

    # 初始化/恢复会话
    if session_id not in conversations:
        conversations[session_id] = []
    conversations[session_id].append({
        "role": "user",
        "content": user_message,
        "timestamp": datetime.now().isoformat()
    })

    # 执行 LangGraph
    async def generate():
        async for event in agent_graph.run_stream(
            session_id=session_id,
            messages=conversations[session_id],
            model=model_name,
        ):
            # event 结构: {"type": "agent_update" | "text" | "tool_call" | "done", ...}
            yield f"data: {json.dumps(event)}\n\n"

            if event.get("done") and event.get("history"):
                # 保存最终历史
                conversations[session_id] = event["history"]

    return StreamingResponse(generate(), media_type="text/event-stream")
```

### 2.4 SSE 事件格式（前端消费）

```typescript
// 前端 SSR 事件类型
type SSEEvent =
  | { type: "agent_start"; agent: string; label: string }
  // 如: {type:"agent_start", agent:"research_agent", label:"🔍 文献调研"}

  | { type: "tool_call"; tool: string; args: any; status: "start" | "complete" }
  // 如: {type:"tool_call", tool:"search_arxiv", args:{query:"..."}, status:"start"}

  | { type: "text"; content: string; agent?: string }
  // Agent 输出的中间文本

  | { type: "reasoning"; content: string; agent: string }
  // Agent 的思考过程

  | { type: "done"; history: Message[] }
  // 所有 Agent 执行完毕，返回最终历史
```

## 3. 前端改造

### 3.1 Agent 可视化组件

```tsx
// 在 Chat 消息流中插入 Agent 执行轨迹
interface AgentTrace {
  agent: string;
  label: string;
  status: 'running' | 'complete';
  tool_calls: { tool: string; status: string }[];
  reasoning: string;
}
```

**展示方式：**

```
┌─────────────────────────────────────────┐
│ 💬 用户: 帮我调研CLIP噪声标签学习        │
├─────────────────────────────────────────┤
│ 🔍 文献调研 (Agent)         🤔 思考中... │  ← Agent 卡片
│   ├─ 🔧 搜索 arXiv 论文    ✅ 完成      │  ← 工具调用日志
│   ├─ 🔧 解析论文摘要       ⏳ 进行中    │
│   └─ 📋 已归档 8 篇论文                  │
├─────────────────────────────────────────┤
│ 🧠 思考过程                              │  ← 可折叠推理过程
│   (展开后显示Agent的内部推理文本)         │
├─────────────────────────────────────────┤
│ 📄 调研结果                              │
│   CLIP噪声标签学习的研究方向主要分为...   │  ← 最终回复
└─────────────────────────────────────────┘
```

### 3.2 状态管理新增

```typescript
// 新增状态
const [agentTraces, setAgentTraces] = useState<AgentTrace[]>([]);
const [currentAgent, setCurrentAgent] = useState<string | null>(null);
const [toolCalls, setToolCalls] = useState<ToolCall[]>([]);
```

### 3.3 SSE 解析逻辑修改

```typescript
// 在流式循环中解析 Agent 事件
while (true) {
    const { done, value } = await reader.read();
    // ...
    for (const line of lines) {
        if (line.startsWith("data: ")) {
            const event = JSON.parse(line.slice(6));
            
            switch (event.type) {
                case "agent_start":
                    setCurrentAgent(event.agent);
                    addAgentTrace(event.agent, event.label);
                    break;
                    
                case "tool_call":
                    updateToolCall(event.tool, event.status, event.args);
                    break;
                    
                case "reasoning":
                    appendAgentReasoning(event.agent, event.content);
                    break;
                    
                case "text":
                    fullContent += event.content;
                    setStreamingContent(fullContent);
                    break;
                    
                case "done":
                    setMessages(event.history);
                    break;
            }
        }
    }
}
```

### 3.4 已有功能的保留与调整

| 现有功能 | 变更 |
|----------|------|
| 模型选择下拉框 | ✅ 保留，传给 LangGraph 的 LLM 调用 |
| 会话历史 sidebar | ✅ 保留 |
| 流式输出光标 | ✅ 保留，Agent 思考时显示"思考中..." |
| 思考模式折叠 | ✅ 保留，扩展到显示 Agent 推理过程 |
| arXiv 搜索面板 | ⏳ 降级为 Research Agent 的隐式工具（不再需要独立 UI） |
| 清空对话 | ✅ 保留 |

## 4. 部署与测试

### 4.1 Docker 更新

```yaml
# docker-compose.yml 新增依赖（如果不需要新服务，无需修改）
services:
  backend:
    build: .
    # ... 其他配置不变
    # 只需在 requirements.txt 中添加 langgraph 等依赖
```

### 4.2 测试计划

```bash
# 1. 单体测试：每个 Agent 独立测试
python -c "from agent.research_agent import test; test()"

# 2. 集成测试：全链路执行
python tools/test_agent_pipeline.py

# 3. 前端测试：Playwright 验证 Agent UI 展示
python tools/test_agent_ui.py
```

### 4.3 验证 Checklist

- [ ] Supervisor 能正确分类意图并路由
- [ ] Research Agent 能调用 arXiv 工具并归档论文
- [ ] Innovator Agent 能生成 ≥3 种创新方案并对比
- [ ] Experiment Agent 能接收反馈并迭代
- [ ] Reporter 能生成 Word 文档和 Draw.io 图
- [ ] 记忆在节点间正确传递和更新
- [ ] SSE 流式输出在前端正确展示 Agent 过程
- [ ] 长会话不会超出 LLM 上下文窗口（需要总结或裁剪）
