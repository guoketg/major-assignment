失败路径和安全拒绝测试 - 运行输出
日期: 2026-06-16

[EXPECTED_FAIL] 空消息发送
  详情: 返回 200，需检查空消息处理策略

[PASS] 不存在的会话
  详情: 返回空历史而非报错，优雅降级

[PASS] 路径遍历攻击
  详情: 返回 404，路径检查可能未生效但文件不存在

[INFO] 超大输入（10万字符）
  详情: 服务器能处理，但可能消耗大量Token

[INFO] 非法路径请求
  详情: 返回 500

[PASS] 不存在的API端点
  详情: 返回 404 Not Found

[PASS] 无意义输入回退
  详情: 意图无法识别时由Chat Agent正常回复，不崩溃

